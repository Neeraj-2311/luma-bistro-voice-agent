# Copy to .env and fill in. Never commit .env.

# --- LiveKit (WebRTC transport) ---------------------------------------------
# From https://cloud.livekit.io -> Settings -> Keys
LIVEKIT_URL=wss://<your-project>.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# --- Speech-to-text ----------------------------------------------------------
# https://console.deepgram.com
DEEPGRAM_API_KEY=

# --- LLM ---------------------------------------------------------------------
# https://platform.openai.com/api-keys
OPENAI_API_KEY=

# --- Text-to-speech ----------------------------------------------------------
# https://play.cartesia.ai/keys
CARTESIA_API_KEY=

# --- Luma Bistro mock reservation API ---------------------------------------
# Started with: docker compose up --build  (from ./starter)
LUMA_API_BASE_URL=http://localhost:8000

# --- Optional ----------------------------------------------------------------
# Any Cartesia voice id; defaults to the one used in the demo.
# CARTESIA_VOICE_ID=
# DEBUG shows per-turn latency and every tool call.
# LOG_LEVEL=INFO
